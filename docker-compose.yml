version: '3.8'

services:
  backend:
    build: ./src/backend
    ports:
      - "8000:8000"
    env_file: ./src/backend/.env
    volumes:
      - ./logs:/app/logs

  frontend:
    build: ./src/frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
